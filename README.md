# validator-inputs
Dynamic form validation system for multiple input types

Supports real-time and on-submit validation for text, number, select, radio, and checkbox fields — using HTML data-* attributes for easy configuration.
